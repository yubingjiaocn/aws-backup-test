# AWS Backup for EKS 功能测试指南

本指南用于测试 AWS Backup 对 EKS 集群的备份和恢复功能。

## 📋 测试目标

验证 AWS Backup 在以下两个场景中的能力:

1. **场景一: EKS 版本回滚** - 升级失败后恢复到旧版本
2. **场景二: 操作错误恢复** - 误删除资源后的数据恢复

## ⏱️ 预计时间

- **完整测试**: 2-4 小时
- **成本**: 约 $1-2 美元 (测试后立即清理)

## 🔧 前置条件

### 1. 检查工具

```bash
# 检查必需工具是否已安装
command -v aws && echo "✅ AWS CLI"
command -v kubectl && echo "✅ kubectl"
command -v eksctl && echo "✅ eksctl"
command -v terraform && echo "✅ Terraform"
command -v jq && echo "✅ jq"
command -v helm && echo "✅ Helm"
```

如果缺少工具,请先安装:
- AWS CLI: https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html
- kubectl: https://kubernetes.io/docs/tasks/tools/
- eksctl: https://eksctl.io/installation/
- Terraform: https://developer.hashicorp.com/terraform/install
- jq: `sudo apt install jq` 或 `brew install jq`
- Helm: https://helm.sh/docs/intro/install/

### 2. 配置 AWS 凭证

```bash
# 配置 AWS 凭证
aws configure

# 验证身份
aws sts get-caller-identity
```

### 3. 确认 IAM 权限

测试账户需要以下权限:
- EKS 完整权限
- EC2 完整权限 (VPC, 子网, NAT 网关等)
- IAM 角色创建和管理
- AWS Backup 完整权限
- EFS 完整权限

## 🚀 测试步骤

### 第一阶段: 环境准备

#### 步骤 1: 创建基础设施 (Terraform)

```bash
cd /home/ubuntu/aws-backup-test/terraform

# 初始化 Terraform
terraform init

# 查看将要创建的资源
terraform plan

# 创建基础设施 (约 5-7 分钟)
terraform apply -auto-approve
```

**创建的资源**:
- ✅ VPC (包含公有和私有子网)
- ✅ NAT 网关 (单个,成本优化)
- ✅ IAM 角色 (使用 Pod Identity)
- ✅ EFS 文件系统
- ✅ 安全组

#### 步骤 2: 创建 EKS 集群 (eksctl)

```bash
cd /home/ubuntu/aws-backup-test

# 生成 eksctl 配置
./eksctl-config/export-tf-outputs.sh 1.32

# 创建 EKS 集群 (约 15-20 分钟)
eksctl create cluster -f eksctl-config/cluster-generated.yaml

# 配置 kubectl
CLUSTER_NAME=$(cd terraform && terraform output -raw cluster_name)
aws eks update-kubeconfig --name $CLUSTER_NAME --region us-west-2
```

**等待集群就绪**:
```bash
# 验证集群
kubectl get nodes
kubectl get pods -A
```

#### 步骤 3: 启用托管 Add-on 和部署测试工作负载

```bash
cd /home/ubuntu/aws-backup-test

# 获取集群名称
CLUSTER_NAME=$(cd terraform && terraform output -raw cluster_name)

# 启用 EBS 和 EFS CSI Driver (约 3-5 分钟)
./scripts/03-enable-managed-addons.sh $CLUSTER_NAME

# 部署测试命名空间和工作负载
kubectl apply -f test-workloads/namespace.yaml
kubectl apply -f test-workloads/statefulset-ebs.yaml

# 更新 EFS ID 并部署 EFS 工作负载
EFS_ID=$(cd terraform && terraform output -raw efs_filesystem_id)
sed -i "s/fs-XXXXXXXXX/$EFS_ID/" test-workloads/deployment-efs.yaml
kubectl apply -f test-workloads/deployment-efs.yaml

# 安装 Karpenter (约 2-3 分钟)
./scripts/04-install-karpenter.sh $CLUSTER_NAME

# 部署 Karpenter NodePool
kubectl apply -f test-workloads/karpenter-nodepool.yaml
```

**验证部署**:
```bash
# 检查所有 Pod 是否运行
kubectl get pods -n test
kubectl get pods -n karpenter

# 检查 PVC 是否绑定
kubectl get pvc -n test
```

### 第二阶段: 场景一测试 - EKS 版本回滚

#### 步骤 4: 创建备份

```bash
# 创建集群备份 (约 5-10 分钟)
./scripts/01-create-backup.sh $CLUSTER_NAME

# 等待备份完成,记录 Recovery Point ARN
# 输出示例: arn:aws:backup:us-west-2:123456789012:recovery-point:xxxxx
```

**保存 Recovery Point ARN** - 下一步需要使用!

#### 步骤 5: 模拟升级失败,恢复到新集群

```bash
# 恢复到新的 v1.32 集群 (约 20-25 分钟)
./scripts/02-restore-to-new-cluster.sh \
  --recovery-point-arn <粘贴上一步的 ARN> \
  --cluster-name eks-rollback-v132 \
  --eks-version 1.32

# 等待恢复完成...
```

#### 步骤 6: 在新集群上配置 Add-on

```bash
# 启用 CSI Drivers
./scripts/03-enable-managed-addons.sh eks-rollback-v132

# 重新安装 Karpenter Controller
./scripts/04-install-karpenter.sh eks-rollback-v132
```

#### 步骤 7: 验证恢复结果

```bash
# 运行验证脚本
./scripts/05-verify-restore.sh eks-rollback-v132
```

**验证内容**:
- ✅ 集群版本正确
- ✅ 所有节点健康
- ✅ 命名空间已恢复
- ✅ Deployment 和 StatefulSet 已恢复
- ✅ PVC 已绑定
- ✅ Karpenter CRD 和 CR 已恢复
- ✅ 数据完整性

### 第三阶段: 场景二测试 - 操作错误恢复

#### 步骤 8: 模拟操作错误

```bash
# 切换到原始集群
aws eks update-kubeconfig --name $CLUSTER_NAME --region us-west-2

# 模拟误删除
kubectl delete namespace test
kubectl delete -f test-workloads/karpenter-nodepool.yaml

# 确认资源已删除
kubectl get namespace test  # 应该显示 NotFound
kubectl get nodepools -n karpenter  # 应该为空
```

#### 步骤 9: 从备份恢复

```bash
# 使用 AWS Backup 恢复到同一集群 (非破坏性)
# 注意: 这是就地恢复,不会覆盖现有对象

aws backup start-restore-job \
  --recovery-point-arn <备份的 Recovery Point ARN> \
  --metadata \
    clustername=$CLUSTER_NAME,\
    newCluster=false,\
    restoreOnlyClusterState=true \
  --iam-role-arn arn:aws:iam::$(aws sts get-caller-identity --query Account --output text):role/AWSBackupDefaultServiceRole \
  --region us-west-2

# 等待恢复完成 (约 5-10 分钟)
# 查询恢复作业状态
aws backup describe-restore-job \
  --restore-job-id <作业 ID> \
  --region us-west-2
```

#### 步骤 10: 验证恢复

```bash
# 检查命名空间是否恢复
kubectl get namespace test

# 检查 Pod 是否恢复
kubectl get pods -n test

# 检查 Karpenter NodePool 是否恢复
kubectl get nodepools -n karpenter

# 验证数据完整性
./scripts/05-verify-restore.sh $CLUSTER_NAME
```

## 🧹 清理资源

**重要**: 测试完成后立即清理,避免产生不必要的费用!

```bash
# 1. 删除所有 EKS 集群
eksctl delete cluster --name $CLUSTER_NAME --region us-west-2 --wait
eksctl delete cluster --name eks-rollback-v132 --region us-west-2 --wait

# 2. 销毁 Terraform 资源
cd /home/ubuntu/aws-backup-test/terraform
terraform destroy -auto-approve

# 3. (可选) 删除 AWS Backup 备份
aws backup list-recovery-points-by-backup-vault \
  --backup-vault-name Default \
  --region us-west-2

# 手动删除备份点 (如需要)
aws backup delete-recovery-point \
  --backup-vault-name Default \
  --recovery-point-arn <ARN> \
  --region us-west-2
```

## ✅ 预期结果

### 场景一: 版本回滚

| 验证项 | 预期结果 |
|--------|---------|
| 集群版本 | ✅ v1.32 |
| 节点状态 | ✅ Ready |
| 命名空间 | ✅ 已恢复 |
| Deployments | ✅ 已恢复 |
| StatefulSets | ✅ 已恢复 |
| PVC | ✅ Bound |
| EBS 数据 | ✅ 自动重新挂载 |
| Karpenter CRDs | ✅ 已恢复 |
| Karpenter CRs | ✅ 已恢复 |
| Karpenter Controller | ⚠️ 需要手动重新安装 |
| CSI Drivers | ⚠️ 需要手动启用 |

### 场景二: 操作错误恢复

| 验证项 | 预期结果 |
|--------|---------|
| 误删除的命名空间 | ✅ 已恢复 |
| 误删除的 Pods | ✅ 已恢复 |
| 误删除的 CRs | ✅ 已恢复 |
| 数据完整性 | ✅ 完整 |
| 恢复方式 | ✅ 非破坏性 |

## ⚠️ 已知限制

1. **版本回滚限制**
   - ❌ 无法在同一集群上降级 Kubernetes 版本
   - ✅ 必须恢复到新集群才能实现版本回滚

2. **需要手动操作**
   - ⚠️ 托管 Add-on (CSI Drivers) 需要手动启用
   - ⚠️ Karpenter Controller 需要重新安装 (CRDs/CRs 会自动恢复)
   - ⚠️ EFS Access Points 需要手动创建

3. **恢复行为**
   - ℹ️ 恢复是非破坏性的,不会覆盖集群中已存在的对象
   - ℹ️ 如果对象已存在,恢复操作会跳过该对象

## 🐛 故障排查

### 问题 1: PVC 一直 Pending

**原因**: CSI Driver 未启用

**解决**:
```bash
./scripts/03-enable-managed-addons.sh <集群名称>
kubectl get pods -n kube-system | grep csi
```

### 问题 2: Karpenter 无法创建节点

**原因**: Karpenter Controller 未安装或 IAM 角色配置错误

**解决**:
```bash
# 检查 Karpenter Pod
kubectl get pods -n karpenter

# 检查日志
kubectl logs -n karpenter -l app.kubernetes.io/name=karpenter

# 重新安装
./scripts/04-install-karpenter.sh <集群名称>
```

### 问题 3: IAM 角色不存在

**原因**: Terraform 未正确创建 IAM 角色

**解决**:
```bash
cd terraform
terraform output cluster_role_arn
terraform output node_role_arn

# 如果输出为空,重新 apply
terraform apply -auto-approve
```

### 问题 4: eksctl 找不到子网

**原因**: eksctl 配置未正确生成

**解决**:
```bash
./eksctl-config/export-tf-outputs.sh 1.32
cat eksctl-config/cluster-generated.yaml
```

### 问题 5: 备份作业失败

**检查**:
```bash
# 查看备份作业详情
aws backup describe-backup-job \
  --backup-job-id <作业 ID> \
  --region us-west-2

# 查看错误消息
aws backup list-backup-jobs \
  --by-backup-vault-name Default \
  --region us-west-2 \
  --query 'BackupJobs[?State==`FAILED`]'
```

## 📊 测试记录模板

```markdown
## 测试记录

**测试日期**: YYYY-MM-DD
**测试人员**: 姓名
**AWS 账户**: 123456789012
**AWS 区域**: us-west-2

### 环境信息
- Terraform 版本:
- eksctl 版本:
- kubectl 版本:
- 源集群版本: v1.32
- 目标集群版本: v1.32

### 场景一: 版本回滚
- [ ] 基础设施创建成功
- [ ] EKS 集群创建成功
- [ ] 测试工作负载部署成功
- [ ] 备份创建成功
- [ ] Recovery Point ARN: _______________
- [ ] 恢复到新集群成功
- [ ] Add-on 配置成功
- [ ] Karpenter 安装成功
- [ ] 验证脚本通过

**问题记录**:


### 场景二: 操作错误恢复
- [ ] 模拟删除操作成功
- [ ] 恢复作业启动成功
- [ ] 恢复作业完成
- [ ] 命名空间恢复
- [ ] Pod 恢复
- [ ] Karpenter CR 恢复
- [ ] 数据完整性验证通过

**问题记录**:


### 成本统计
- 基础设施成本: $___
- EKS 集群成本: $___
- 总测试成本: $___

### 总结


```

## 📞 获取帮助

如果测试过程中遇到问题:

1. 查看脚本输出的详细日志
2. 检查 kubectl 和 AWS CLI 命令输出
3. 查看本文档的"故障排查"部分
4. 保存错误信息和日志以便分析

## 🎯 测试完成标准

- ✅ 场景一: 成功创建备份并恢复到新集群,所有资源正常运行
- ✅ 场景二: 成功从误删除中恢复资源,数据完整
- ✅ 验证脚本全部通过
- ✅ 资源已清理,无遗留费用

祝测试顺利! 🚀
